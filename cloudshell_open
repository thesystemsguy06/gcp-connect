#!/bin/bash
# Superseded. This printed instructions for the VP_TOKEN flow — "click START in
# the tutorial pane", "run ./setup_env.sh" — neither of which is how onboarding
# works now. What Cloud Shell actually shows on open is PRINT.md, named by the
# cloudshell_print parameter in the link VectorPlane generates.
echo "Run ./deploy.sh to connect this project to VectorPlane."
echo "You will need the pairing code shown on your VectorPlane dashboard."
